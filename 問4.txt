SELECT
	page_id,
	referer,
	ip_address
FROM
	access_log
ORDER BY
	# アクセス日時の昇順は古い順に並んでしまう
	access_date ASC
LIMIT
	# 指定が逆。11行目から0件取得になっている
	10,0
;



SELECT
	page_id,
	referer,
	ip_address
FROM
	access_log
ORDER BY
	access_date DESC
LIMIT
	10
;