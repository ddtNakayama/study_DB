SELECT
	depart_id,
	class,
	# カラムの結合ではなく文字列の結合になっている
	CONCAT('l_name', 'f_name') AS 氏名
FROM
	employee
ORDER BY
	# 部署ごとに降順ができていない
	depart_id,
	class DESC
;



SELECT
	depart_id,
	class,
	CONCAT(l_name, f_name) AS 氏名
FROM
	employee
ORDER BY
	depart_id DESC,
	class DESC
;